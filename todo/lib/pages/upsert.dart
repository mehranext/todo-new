import 'package:flutter/material.dart';

class UpsertScreen extends StatelessWidget {
  const UpsertScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
